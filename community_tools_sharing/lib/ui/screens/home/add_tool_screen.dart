import 'package:flutter/material.dart';

class AddToolScreen extends StatelessWidget {
  const AddToolScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Add Tools – TODO: Add UI'));
  }
}
